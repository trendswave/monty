#include "monty.h"

/**
 * nop - This function does nothing
 * @stack: Pointer to the stack
 * @l_num: Line number of file
 * Return: Returns Pointer to the stack
 */

stack_t *nop(stack_t **stack __attribute__ ((unused)),
	  unsigned int l_num __attribute__((unused)))
{
	return (0);
}
